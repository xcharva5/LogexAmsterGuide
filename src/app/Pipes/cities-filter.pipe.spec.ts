import { CitiesFilterPipe } from './cities-filter.pipe';

describe('CitiesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CitiesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
