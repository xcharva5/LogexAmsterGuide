import { SubstringFilterPipe } from './substring-filter.pipe';

describe('SubstringFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubstringFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
