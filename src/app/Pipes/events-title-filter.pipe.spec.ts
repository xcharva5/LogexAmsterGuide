import { EventsTitleFilterPipe } from './events-title-filter.pipe';

describe('EventsTitleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsTitleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
